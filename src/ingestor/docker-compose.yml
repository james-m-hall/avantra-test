services:
  pubsub:
    image: gcr.io/google.com/cloudsdktool/google-cloud-cli:emulators
    restart: always
    ports:
      - "8085:8085"
    command: gcloud beta emulators pubsub start --project=integration-test --host-port=0.0.0.0:8085
